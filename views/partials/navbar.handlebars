    <nav class="navbar navbar-dark bg-dark">
        <div class="container">
            <div class="row">
                <div class="col d-flex justify-content-center">
                <i class="fas fa-basketball-ball" style="color:orange;font-size:32px"></i>
                <img class="" src="dreamteam.png" alt="navbarlogo" height="32">
                </div>
            </div>

            {{#if logged_in}}
            <button class="btn btn-outline-success" type="submit" id="logout">Logout</button>
            {{else}}
            <button class="btn btn-outline-success" type="submit" id="login">Login</button>
            {{/if}}
        </div>
    </nav>
